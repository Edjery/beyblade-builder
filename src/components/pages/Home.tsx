import { Box } from "@mui/material";
import { Fragment } from "react";

export const Home = () => {
  // Welcome screen in the middle
  // Log in / Sign up
  // Example input output

  return (
    <Fragment>
      <Box>Welcome!</Box>
      <Box>Log in / Sign Up</Box>
      <Box>Sample Builder</Box>
    </Fragment>
  );
};
